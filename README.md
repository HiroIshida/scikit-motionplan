## installation

```bash
sudo apt-get update
sudo apt-get install libspatialindex-dev freeglut3-dev libsuitesparse-dev libblas-dev liblapack-dev

git clone git@github.com:HiroIshida/scikit-motionplan.git
cd scikit-motionplan
pip3 install -e .
```
